# 작업 일지 — 2026-06-30

Plan 3·4·5를 하루에 spec → plan → 구현 → 프로덕션 검증까지 모두 완료. SaaS 측 모든 매체(카페24·스마트스토어·네이버광고) 자격증명 등록 흐름 + sync 인프라 + 카페24 첫 실 데이터 동기화까지 도달. order-manager-saas 레포에 33 커밋, order-manager(spec/plan/worklog) 레포에 6 커밋.

---

## 진행 흐름 한눈에

```
Plan 3 spec → plan → SDD 구현 → 검증 (스마트스토어/네이버광고 어댑터)
  → Plan 4 spec → plan → SDD 구현 → 검증 (sync 인프라 + 가상서버 sync-worker)
  → Plan 5 spec → plan → SDD 구현 → 검증 (카페24 sync 메서드)
  → cleanup + 워크로그
```

레포 위치:
- 신규 SaaS: `C:\Users\Jangkwon\Desktop\order-manager-saas` (Production: https://order-manager-saas-bay.vercel.app)
- 가상서버: `203.245.41.105` `/root/sync-worker/` (신규 PM2 프로세스 2개 + cloudflared quick tunnel 1개)
- 기존 레포: `c:\Users\Jangkwon\Desktop\order-manager` (spec/plan/worklog 문서 보관)

---

## Plan 3 — 스마트스토어 + 네이버광고 어댑터

### Spec / Plan (커밋 `011d9d1`, `b3694e6`)

`docs/superpowers/specs/2026-06-30-plan3-smartstore-naverad-design.md` (418줄), `docs/superpowers/plans/2026-06-30-plan3-smartstore-naverad.md` (1078줄).

### 결정 사항

| 항목 | 결정 |
|---|---|
| 매체별 channel_account | 사용자 입력 별칭 (예: "테스트스토어", "주력광고계정") |
| 어댑터 인터페이스 확장 | `credentialFields: FieldDef[]` + `buildPayload(formValues)` 옵셔널 추가 |
| UI 라우트 | cafe24는 기존 그대로, smartstore/naver_ad는 `[channel]/new` 동적 폼 페이지 1개로 통합 |
| validate 호출 위치 | 처음엔 Vercel 직접 → 프로덕션 검증 시 IP whitelist로 막힘 → Plan 4 hotfix로 가상서버 경유 전환 |
| 등록 API | `POST /api/credentials/register/[channel]` (slug 충돌 회피로 path 재구성 hotfix) |

### 구현 (Task 1~7, 커밋 `071a830` ~ `00bbd5d` + slug hotfix `f7a26dc`)

`lib/adapters/_types.ts` 확장(SyncContext는 Plan 4에서) + `lib/adapters/smartstore.ts` + `lib/adapters/naver-ad.ts` + `[channel]/new` 동적 폼 + `register endpoint` + connections 카드 활성. 

### 검증

- 스마트스토어/네이버광고 카드 활성 ✅
- Vercel 직접 호출은 `GW.IP_NOT_ALLOWED` (가상서버 IP만 화이트리스트) — 로컬 dev에서는 사용자 로컬 IP가 등록되어 있어 통과 → 검증 자체는 완료, 프로덕션 통과는 Plan 4에서 해소
- Vault payload 6필드(스마트스토어 2개 + 네이버광고 3개) 정상 저장

---

## Plan 4 — sync 인프라 골격 + Plan 3 validate hotfix

### Spec / Plan (커밋 `0925149`, `7f1b0f1`)

`docs/superpowers/specs/2026-06-30-plan4-sync-infra-validate-hotfix-design.md` (565줄), `docs/superpowers/plans/2026-06-30-plan4-sync-infra-validate-hotfix.md` (1718줄).

### 아키텍처

```
Supabase
├── pg_cron 6 잡 (orders/products/ad_stats/ad_units 매체별 + token_refresh)
├── sync_jobs 큐 (Plan 1에서 만든 테이블 그대로 사용)
└── RPC: read_vault_secret, pick_pending_job, reset_stale_running

가상서버 /root/sync-worker/
├── worker.js (PM2: sync-worker) — 5초 polling, FOR UPDATE SKIP LOCKED, 어댑터 라우팅
├── validate-server.js (PM2: validate-proxy) — POST /validate/:channel HTTP 서버
├── lib/supabase.js + lib/adapters.js (stub) + lib/job-type-routing.js
└── 추가 PM2: validate-tunnel (cloudflared quick tunnel)

Vercel
├── lib/adapters/smartstore.ts, naver-ad.ts — validate가 가상서버 경유로 hotfix
├── lib/adapters/_types.ts — SyncContext / SyncResult / RefreshResult + 5개 옵셔널 sync 메서드 추가
└── 환경변수 신규: VALIDATE_PROXY_URL, VALIDATE_PROXY_TOKEN
```

### 구현 (Task 1~8, 커밋 `c58898f` ~ `b398d41`)

| Task | 내용 | 커밋 |
|---|---|---|
| 1 | .env.example 환경변수 추가 | `c58898f` |
| 2 | SQL 마이그레이션 3개 (010 vault read, 011 pg_cron 6잡, 012 sync_jobs picking) | `0d5b2ec` |
| 3 | _types.ts SyncContext/SyncResult/RefreshResult + 5 옵셔널 sync 메서드 | `ac2dfd3` |
| 4 | smartstore/naver-ad validate를 가상서버 경유로 | `a7ea619` |
| 5 | server/sync-worker/ 9 파일 (worker.js, validate-server.js, lib/*, ecosystem, README) | `bc48371` |
| - | dotenv 의존성 누락 fix | `b398d41` |

### Production hotfixes (검증 중 발견)

| 커밋 | 내용 |
|---|---|
| Next.js dynamic route slug 충돌 fix | `f7a26dc` (Plan 3에 보고) — `/api/credentials/[credentialId]/delete` vs `/api/credentials/[channel]/register` slug 충돌 → register를 `/api/credentials/register/[channel]`로 이동 |
| HTTP 431 (쿠키 누적) | localhost 다중 Supabase 세션. 시크릿 창으로 회피 (코드 변경 X) |
| `OAUTH_COOKIE_SECRET` Vercel 미적용 | 환경변수 추가 후 캐시 무효화 재배포 필요. 사용자 안내 |
| cafe24 validate 403 (scope 부족) | `/admin/store`는 scope에 없는 권한 → `/admin/categories?limit=1`로 변경 |
| supabase-js `.schema('vault').rpc()` 차단 | PostgREST가 vault schema expose 안 함 → public.create_vault_secret/delete_vault_secret wrapper로 우회 |

### 운영자 가상서버 셋업

- 호스팅 인바운드 차단(SSH만 열림) → IP+포트 직접 옵션 불가
- 기존 `naver-tunnel`이 cloudflared token-based이지만 사용자가 처음 사용해서 Zero Trust 콘솔 접근 어려움
- 해결: `cloudflared tunnel --url http://localhost:3003 --protocol http2`로 quick tunnel 사용 → PM2 등록(`validate-tunnel`)
- 발급된 URL: `https://minerals-maintain-pts-cocktail.trycloudflare.com` (재시작 시 변경 — Plan 5b/6/7 시점에 named tunnel로 안정화 권장)

### 가상서버 sync-worker crash 디버깅

- Node 20에서 supabase-js의 RealtimeClient가 native WebSocket 부재로 throw → `ws` 패키지 + `realtime: { transport: WebSocket }` 옵션 추가 (`57a444d` hotfix)
- `pick_pending_job` RPC가 큐 비어있을 때 `{id:null, ...}` 반환 → worker가 null job 처리 시도 → `if (!data || !data.id) return` 가드 추가 (같은 hotfix)

### 검증

| 시나리오 | 결과 |
|---|---|
| 스마트스토어 프로덕션 등록 | ✅ (가상서버 경유로 IP whitelist 통과) |
| 네이버광고 프로덕션 등록 | ✅ |
| pg_cron token_refresh 자동 큐잉 | ✅ (5분 boundary 직후 1행 INSERT) |
| sync-worker picking + skipped 처리 | ✅ (Plan 4 단계 어댑터 미구현이라 정상 skip) |

---

## Plan 5 — 카페24 sync 메서드 구현

### Spec / Plan (커밋 `3810d36`, `a3d68bd`)

`docs/superpowers/specs/2026-06-30-plan5-cafe24-sync-design.md` (428줄), `docs/superpowers/plans/2026-06-30-plan5-cafe24-sync.md` (992줄).

### 결정 사항

| 항목 | 결정 |
|---|---|
| 팔레오 컷오버 | Plan 5 미포함, Plan 5b로 분리 (1주 병행 운영 후 안정 확인) |
| 데이터 테이블 | `orders` + `order_items` + `catalog_products` 그대로 사용 (병행 운영 idempotent 보장) |
| 카페24 sync 코드 위치 | 가상서버 `lib/adapters.js` cafe24 stub을 실 구현으로 교체 |
| Vercel cafe24.ts | sync 메서드 시그니처만 (throw — 가상서버 외 호출 차단) |
| metadata.expires_at | Plan 2 callback에서 INSERT 시점에 채움. token_refresh cron 정확도 ↑ |

### 구현 (Task 1~7, 커밋 `bfb476f` ~ `9c3b99a`)

| Task | 내용 | 커밋 |
|---|---|---|
| 1 | `013_vault_update_wrapper.sql` (update_vault_secret RPC) | `bfb476f` |
| 2 | cafe24.ts에 sync 메서드 시그니처 추가 (throw) | `3f00855` |
| 3 | callback에 `metadata.expires_at: payload.expiresAt` 추가 | `e0e441b` |
| 4 | 가상서버 cafe24Adapter.refreshToken 실 구현 (https request + Basic auth) | `fa072b6` |
| 5 | 가상서버 cafe24Adapter.syncProducts (페이지네이션 + catalog_products upsert) | `7e24a05` |
| 6 | 가상서버 cafe24Adapter.syncOrders (페이지네이션 + orders upsert + order_items DELETE+INSERT) | `5cdbc66` |
| 7 | worker.js token_refresh result 분기 (Vault update + metadata 동기화) | `9c3b99a` |

### Production hotfixes (검증 중 발견)

검증 중에 catalog_products와 orders 테이블의 실제 컬럼이 spec/plan에 박은 가정과 달라서 두 차례 fix:

| 커밋 | 내용 |
|---|---|
| `9cdea0c` | `catalog_products` 실 컬럼에 매핑 수정: `selling_price` → `price`, `display`/`selling` 컬럼 제거, `synced_at` → `updated_at`, small_image/summary_description/manufacturer/weight 추가 |
| `6569fa8` | `orders` 실 컬럼에 매핑 수정: `member_id`/`status`/`payment_date`/`synced_at` 제거, `mall_type:'cafe24'` 추가, `total_qty`/`original_amount`/`is_cancelled`/`is_new` 매핑. `order_items`는 `quantity`→`qty`, `price`→`amount`, product_no/variant_code 제거 |

### 검증

`https://order-manager-saas-bay.vercel.app`에서 운영자가 새 시크릿 창으로:

| 시나리오 | 결과 |
|---|---|
| D1 — callback `metadata.expires_at` 채움 | ✅ `{"scope":"...","expires_at":"2026-06-30T12:11:27.000"}` |
| D2 — syncProducts 수동 enqueue → catalog_products | ✅ `rowsUpserted:100`, 100행 (페이지 1 분량) |
| D3 — syncOrders 수동 enqueue → orders/order_items | ✅ orders 25행 + order_items 38행 |

SaaS 파이프라인(pg_cron → sync_jobs → 가상서버 worker → cafe24 API → upsert)이 처음으로 실 데이터를 가지고 끝까지 흘렀음.

---

## DB 스키마 / RLS 변경 (오늘 적용)

### Supabase 마이그레이션 (사용자 SQL Editor 실행)

| 파일 | 내용 |
|---|---|
| `010_vault_read_wrapper.sql` | `public.read_vault_secret` RPC |
| `011_pg_cron_jobs.sql` | 6 cron 잡 (cafe24/smartstore orders 30분, cafe24/naver_ad products+units 매일 03시, naver_ad stats 매 12h, token_refresh 매 5분) |
| `012_sync_job_rpcs.sql` | `pick_pending_job` (SELECT FOR UPDATE SKIP LOCKED) + `reset_stale_running` |
| `013_vault_update_wrapper.sql` | `public.update_vault_secret` RPC |

### pg_cron extension 활성화

- 처음엔 `cron` schema 없음 에러 → Supabase 대시보드 Database → Extensions에서 pg_cron 토글 ON
- 활성 후 011 재실행 성공

### 외부 인프라 변경

- 가상서버 신규 PM2 프로세스 3개: `sync-worker`, `validate-proxy`, `validate-tunnel` (cloudflared)
- 가상서버 `/root/sync-worker/.env` (chmod 600): SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, VALIDATE_PROXY_PORT=3003, VALIDATE_PROXY_TOKEN

### Vercel 환경변수 추가

- `VALIDATE_PROXY_URL=https://minerals-maintain-pts-cocktail.trycloudflare.com`
- `VALIDATE_PROXY_TOKEN=uWSv7Dl1ARkHRgTTERrW0xhpO/U7lMZk4mdla1MSMek=`

---

## 미해결 / 다음 작업 대기

### Plan 5b — 팔레오 카페24 컷오버 (1주 후)

새 sync-worker가 1주 안정 운영 후:
1. 데이터 일치 확인 (기존 sync.js와 새 sync-worker가 같은 catalog_products/orders에 upsert)
2. 기존 가상서버 `server/sync.js` 카페24 cron 제거
3. 기존 `.env`의 PALEO_APP_ID 등 1주 보존 후 정리

### Plan 6 — 스마트스토어 sync

`lib/adapters/smartstore.ts` + 가상서버 `adapters.js`에 syncOrders + syncProducts 구현. access_token 캐시 패턴 결정. Plan 5와 같은 패턴.

### Plan 7 — 네이버광고 sync

syncAdStats + syncAdUnits. HMAC 서명 매 요청. `ad_stats`/`ad_units` 테이블 upsert.

### cloudflared quick tunnel 안정화

`validate-tunnel`은 PM2 재시작 시 URL 변경 → Vercel env 재등록 부담. Plan 5b/6 시점에 다음 중 하나:
- Cloudflare 무료 계정 + named tunnel
- 가상서버 nginx + Let's Encrypt + 도메인 (사용자 보유 도메인 필요)

### Plan 4의 nice-to-fix

- worker.js token_refresh 분기에서 brand_credentials UPDATE 두 번 → 한 번에 합치기 (효율)
- adapters.js 일부 미사용 import (https 직접 안 쓰는 곳)
- 카페24 sync 메서드의 변수 shadow (refreshToken local var)

---

## 워크플로 메모

- 하루에 plan 3개 spec → plan → SDD → 검증을 완주. SDD 패턴이 안정적이고 plan 작성이 자세하면 implementer subagent가 거의 한 번에 통과.
- Plan 5의 컬럼 매핑은 spec/plan 단계에서 결정 못한 부분(기존 sync.js와 다른 테이블 구조). 검증 단계에서 두 번 fix. Plan 6/7도 같은 위험 — 검증 시점에 매체별 응답·DB 컬럼 직접 보고 매핑.
- 가상서버 환경(인바운드 차단 + token tunnel)이 진입 장벽. cloudflared quick tunnel로 회피했지만 운영 안정성 ↓. 매체 sync가 본격 가동되기 전에 안정화 필요.
- 사용자가 SSH/PM2/nano/curl/SQL Editor를 모두 다루는 흐름. 한 단계씩 자세히 안내가 효과적 (vs 한 번에 묶음). "내가 해야 할 것만 명확히" 같은 사용자 피드백으로 안내 형식 조정.
- "오늘 작업종료" 명령을 명시 안 해도 cleanup 결과 보고로 자연스럽게 마무리. 사용자 짧은 답변 패턴.
